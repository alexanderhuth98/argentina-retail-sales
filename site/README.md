# Sitio estático generado

No edite `index.html`, `mobile.html` ni `assets/` manualmente. Regenerar desde los cinco CSV curados con:

```powershell
argentina-retail-sales export
```

Plotly Basic 2.35.2 y las fuentes locales se copian desde el paquete y se validan contra los SHA-256 declarados en `dashboard.py`.
